# ChocapicShaderPatcher
A small patcher to make Chocapic13 Shaders work with End fog colors and correct light in the End

## Usage:
Run command in the console/terminal: java -jar file-name.jar path-to-shaders.zip